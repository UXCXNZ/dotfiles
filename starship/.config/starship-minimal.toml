# Minimal Starship config for use with tmux
# Since tmux status bar shows directory/git info, keep prompt simple

format = "$character"

[character]
success_symbol = '[❯](bold green)'
error_symbol = '[❯](bold red)'
vimcmd_symbol = '[❮](bold green)'